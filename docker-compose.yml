version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: taskhub_postgres
    environment:
      POSTGRES_DB: taskhub
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - taskhub_network

  app:
    build: .
    container_name: taskhub_app
    ports:
      - "8080:8080"
    depends_on:
      - postgres
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: postgres
      DB_PASSWORD: password
      DB_NAME: taskhub
      APP_PORT: 8080
    networks:
      - taskhub_network

volumes:
  postgres_data:

networks:
  taskhub_network:
    driver: bridge